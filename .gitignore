# [수정] 모든 JSON을 막지 말고, 보안 파일만 정확히 지정하세요
service-account.json
# 만약 secrets 폴더 안에 json들을 넣으셨다면 아래처럼
# secrets/*.json

# 나머지 설정은 유지
PlayGround/
.streamlit/
.venv/
venv/
ENV/
# 업무 로그 파일 제외 (보안 및 개인정보 보호)
pages/morning_log.csv
*.csv